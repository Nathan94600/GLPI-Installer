Permet d'installer GLPI facilement grâce à un script

1. Mettre le script sur votre machine
2. Rendre le script exécutable :

```bash
sudo chmod +x install_glpi.sh
```

3. Exécuter le script :

```bash
sudo ./install_glpi.sh
```
